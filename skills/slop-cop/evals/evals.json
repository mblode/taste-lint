{
  "skill_name": "slop-cop",
  "evals": [
    {
      "id": 1,
      "prompt": "Lint the marketing copy under apps/web/app/(marketing) with slop-cop and tell me what it would cost before you run it.",
      "expected_output": "Runs `npx slop-cop lint apps/web/app/(marketing) --dry-run` first, quotes the unit count, request count and estimated cost, then runs the lint only after that, and summarises findings by severity and band with the scorecard delta.",
      "files": [],
      "assertions": [
        "Runs a --dry-run before any live run",
        "Quotes the estimated request count and cost from the dry run",
        "Reports act and review findings separately"
      ]
    },
    {
      "id": 2,
      "prompt": "slop-cop reported [MAJOR?] copywriting-vague-error (p=0.52) on the checkout error string. Is that a blocker?",
      "expected_output": "Explains that MAJOR is the severity if real and the ? marks the review band, so the run does not fail on it; recommends reading the string and either fixing it or adding it to the corpus with the right label, and does not downgrade the severity because the probability is middling.",
      "files": [],
      "assertions": [
        "Distinguishes severity from band",
        "Does not lower the severity to express uncertainty",
        "Suggests a corpus label or a fix rather than editing the threshold by hand"
      ]
    },
    {
      "id": 3,
      "prompt": "Fix every typography-straight-quotes finding in docs/, including the ones inside code fences.",
      "expected_output": "Points out that slop-cop does not report straight quotes inside code fences or inline code because they are correct there, applies --fix or edits only the prose findings, and leaves code untouched.",
      "files": [],
      "assertions": [
        "Refuses to curl quotes inside code",
        "Uses the deterministic fix or edits only reported prose units",
        "Re-runs the lint on the changed files"
      ]
    }
  ],
  "routing": {
    "should_trigger": [
      "Run slop-cop over the content folder",
      "Lint the copy in this PR for hedges and unsupported claims",
      "Check the typography in these components with the taste linter"
    ],
    "near_miss": [
      {
        "prompt": "Audit the font loading and fallback stacks on the site",
        "expected": "typography-audit"
      },
      {
        "prompt": "Rewrite this landing page hero so it leads with the reader",
        "expected": "copywriting"
      }
    ]
  }
}
