---
"taste-lint": patch
---

Add taste-lint init to detect the package manager, install locally, and add local and AI scan scripts. Setup supports a dry run, preserves existing scripts, and can append agent guidance.
