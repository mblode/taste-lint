---
"slop-cop": patch
---

`slop-cop schema` prints the command surface as JSON, and `--output json` turns an error into a JSON envelope on stdout. A Release workflow publishes over npm OIDC from `main`.
