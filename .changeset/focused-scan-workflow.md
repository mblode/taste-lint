---
"taste-lint": patch
---

Add goal-specific scans with scope previews, stable content fingerprints, compatible baselines, reasoned review decisions, changed-code SARIF reporting, and remediation exports. Add blind calibration sample export with validated human-label import and a dependency-cruiser JSON adapter.

Give repetition judgments bounded Markdown section context while preserving cached answers across separate context requests. Treat quotes and sentence length as advisory in writing scans, and recognize README and personal prose for those checks.
