# Hand-authored for taste-lint's code profile from the rule's source in mblode/agent-skills.
id: engineering-test-self-comparison
title: Test compares a value with itself
categoryId: test-value
source:
  repo: mblode/agent-skills
  path: skills/test-audit/SKILL.md
  line: 48
scope:
  include:
    - "**/*.{test,spec}.{ts,tsx,js,jsx,mts,mjs,cts,cjs}"
    - "**/__tests__/**/*.{ts,tsx,js,jsx,mts,mjs}"
unit:
  - source
mechanical:
  regex: \bexpect\(\s*([\w$]+)\s*\)\s*\.\s*(?:toBe|toEqual|toStrictEqual)\(\s*\1\s*\)|\bassert\.(?:equal|strictEqual|deepEqual|deepStrictEqual)\(\s*([\w$]+)\s*,\s*\2\s*[,)]
  codeOnly: true
preconditions:
  notGenerated: true
severity: major
fix:
  hint: Compare against an expected value written independently of the code under test. A value always equals itself.
status: review-only
review:
  applicability: The expected value is the same expression as the actual value.
  exceptions: Tests of equality semantics themselves, such as Object.is or a custom equals.
  evidence: The assertion and what behaviour it was meant to check.
  verification: Replace the expected value with an independent literal and confirm the test fails when the code breaks.
  sourceHash: 6076596823eef4018e5d7215fac7707b71ec10fc5858998b9fce47b80c9743c6
handWritten:
  - mechanical
  - fix
  - scope
