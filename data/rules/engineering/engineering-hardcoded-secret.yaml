# Hand-authored for taste-lint's code profile from the rule's source in mblode/agent-skills.
id: engineering-hardcoded-secret
title: Secret committed in source
categoryId: sensitive-data
source:
  repo: mblode/agent-skills
  path: skills/tidy/references/security-checklist.md
  line: 49
scope:
  include:
    - "**/*.{ts,tsx,js,jsx,mts,mjs,cts,cjs}"
    - "**/*.{json,yml,yaml,toml}"
    - "**/.env"
    - "**/.env.*"
  exclude:
    - "**/*.d.ts"
    - "**/*.min.js"
    - "**/*.gen.*"
    - "**/*.generated.*"
    - "**/generated/**"
    - "**/__generated__/**"
    - "**/vendor/**"
    - "**/fixtures/**"
    - "**/__fixtures__/**"
    - "**/migrations/**"
    - "**/package-lock.json"
    - "**/pnpm-lock.yaml"
    - "**/.env.example"
    - "**/.env.sample"
unit:
  - source
mechanical:
  regex: \b(?:sk_live_[0-9a-zA-Z]{16,}|rk_live_[0-9a-zA-Z]{16,}|AKIA[0-9A-Z]{16}|ghp_[0-9A-Za-z]{36}|gho_[0-9A-Za-z]{36}|github_pat_[0-9A-Za-z_]{40,}|xox[baprs]-[0-9A-Za-z-]{10,}|AIza[0-9A-Za-z_-]{35}|sk-(?:proj-|ant-api\d\d-)?[A-Za-z0-9_-]{40,}|glpat-[0-9A-Za-z_-]{20})\b|-----BEGIN (?:RSA |EC |OPENSSH |DSA |ENCRYPTED )?PRIVATE KEY-----
preconditions:
  notGenerated: true
severity: major
fix:
  hint: Revoke the key now, since history keeps it. Then read it from the environment or a secret manager and add the file pattern to .gitignore.
status: review-only
review:
  applicability: A string matches a real provider's key format.
  exceptions: Documented test keys (sk_test_, example keys in docs) and revoked sample keys in fixtures.
  evidence: The provider, whether the key is live, and whether it is in Git history.
  verification: Revoke the key, move it to the environment, and scan history.
  sourceHash: 59bfe3831e3d608f088cb0792dbc2d26edc745edc51c4afaff12b7cd69bcf6ea
handWritten:
  - mechanical
  - fix
  - scope
