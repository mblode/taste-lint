# Hand-authored for taste-lint's code profile from the rule's source in mblode/agent-skills.
id: engineering-catch-rethrow
title: Catch block that only rethrows
categoryId: code-quality
source:
  repo: mblode/agent-skills
  path: skills/tidy/references/ai-slop-patterns.md
  line: 72
scope:
  include:
    - "**/*.{ts,tsx,js,jsx,mts,mjs,cts,cjs}"
  exclude:
    - "**/*.d.ts"
    - "**/*.min.js"
    - "**/*.gen.*"
    - "**/*.generated.*"
    - "**/generated/**"
    - "**/__generated__/**"
    - "**/vendor/**"
    - "**/fixtures/**"
    - "**/__fixtures__/**"
    - "**/migrations/**"
unit:
  - source
mechanical:
  regex: \bcatch\s*\(\s*(\w+)\s*\)\s*\{\s*throw\s+\1\s*;?\s*\}
preconditions:
  notGenerated: true
severity: minor
fix:
  hint: Delete the try and catch. Rethrowing unchanged adds a level of nesting and nothing else.
status: review-only
review:
  applicability: The catch rethrows the caught value unchanged.
  exceptions: A catch that also runs a finally-like side effect before the throw.
  evidence: The full try and catch block.
  verification: Delete the try and catch and run the tests.
  sourceHash: 2cc5159af691557ba07c0027746413516f4185251dbd6f91980b134ecd8da494
handWritten:
  - mechanical
  - fix
  - scope
