# Hand-authored for taste-lint's code profile from the rule's source in mblode/agent-skills.
id: engineering-test-focused
title: Focused test left in (.only)
categoryId: test-value
source:
  repo: mblode/agent-skills
  path: skills/test-audit/SKILL.md
  line: 47
scope:
  include:
    - "**/*.{test,spec}.{ts,tsx,js,jsx,mts,mjs,cts,cjs}"
    - "**/__tests__/**/*.{ts,tsx,js,jsx,mts,mjs}"
unit:
  - source
mechanical:
  regex: \b(?:describe|it|test|context|suite)\.only\s*\(|\bf(?:it|describe)\s*\(
preconditions:
  notGenerated: true
severity: major
fix:
  hint: Remove .only. It silently skips every other test in the file, and in some runners every other file.
status: review-only
review:
  applicability: The file is a committed test file run by CI.
  exceptions: Local scratch files that are gitignored.
  evidence: The focused call and which tests it silences.
  verification: Remove .only and run the whole file; every test should run.
  sourceHash: 6076596823eef4018e5d7215fac7707b71ec10fc5858998b9fce47b80c9743c6
handWritten:
  - mechanical
  - fix
  - scope
