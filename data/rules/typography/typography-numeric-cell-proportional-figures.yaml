# Sourced from mblode/taste-training apps/web/components/demos/intersections/full-stack-audit.tsx
# (proportional-figures) and agent-skills skills/typography-audit/rules/opentype-tabular-figures.md.
id: typography-numeric-cell-proportional-figures
title: Numbers in a table cell without tabular figures
categoryId: typographic-detail
domain: typography
source:
  {
    repo: mblode/agent-skills,
    path: skills/typography-audit/rules/opentype-tabular-figures.md,
    line: 7,
    tier: HIGH,
  }
scope:
  include: ["**/*.tsx", "**/*.jsx"]
  exclude: ["**/*.test.*", "**/*.spec.*", "**/*.stories.*"]
unit: [class-list]
tier: mechanical
mechanical: { function: numericCellWithoutTabularFigures }
severity: minor
fix:
  mode: none
  hint: Add tabular-nums (font-variant-numeric) to numeric columns so digits share one width and align.
# review-only: tabular-nums inherits, and the check sees only the cell's own
# classes, so a table-level class reads as a miss. Promote once ancestor
# classes reach the unit.
status: review-only
handWritten: [mechanical, fix, status]
