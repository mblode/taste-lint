# Hand-authored for taste-lint's code profile from the rule's source in mblode/agent-skills.
id: seo-canonical-wrong-host
title: Canonical points at a dev or preview host
categoryId: search-visibility
source:
  repo: mblode/agent-skills
  path: skills/seo/references/indexing-policy.md
  line: 6
scope:
  include:
    - "**/*.{tsx,jsx,ts,js}"
    - "**/*.html"
  exclude:
    - "**/*.d.ts"
    - "**/*.min.js"
    - "**/*.gen.*"
    - "**/*.generated.*"
    - "**/generated/**"
    - "**/__generated__/**"
    - "**/vendor/**"
    - "**/fixtures/**"
    - "**/__fixtures__/**"
    - "**/migrations/**"
unit:
  - source
mechanical:
  regex: (?:canonical|metadataBase)[^\n]{0,40}?["'`](?:http://(?!localhost)[^"'`]+|https?://(?:localhost|127\.0\.0\.1|0\.0\.0\.0)[:/][^"'`]*|https://[\w-]+\.vercel\.app[^"'`]*|https://[\w-]+\.netlify\.app[^"'`]*)["'`]
preconditions:
  notGenerated: true
severity: major
fix:
  hint: Point canonical URLs and metadataBase at the production origin over https, or build them from an environment variable set per deployment.
status: review-only
review:
  applicability: A canonical or metadataBase URL is a dev, preview or http origin.
  exceptions: Development-only config files and tests.
  evidence: Where the value is used and which deployments read it.
  verification: Build for production and read the canonical link in the HTML.
  sourceHash: 2f3d680262b93b5ecf37c7780218f03221541b5f2c12dca6d11ed61d898b5a0e
handWritten:
  - mechanical
  - fix
  - scope
