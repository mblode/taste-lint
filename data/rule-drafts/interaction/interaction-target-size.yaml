# Ported from mblode/agent-skills skills/ui-design/rules/interaction-target-size.md by scripts/port-rules.ts.
# Keys listed in handWritten are preserved on re-run; everything else is regenerated.
id: interaction-target-size
title: Meet Minimum Hit Target Size
categoryId: focus-and-a11y
domain: interaction
source:
  line: 9
  path: skills/ui-design/rules/interaction-target-size.md
  repo: mblode/agent-skills
  ruleId: interaction-target-size
  tier: fix-this-sprint
scope:
  exclude:
    - "**/*.test.*"
    - "**/*.spec.*"
    - "**/*.stories.*"
  include:
    - "**/*.tsx"
    - "**/*.jsx"
unit:
  - paragraph
  - heading
  - jsx-text
  - attr-string
tier: jev
question:
  context:
    - element
    - docType
  criteria:
    "false":
      examples:
        - .icon-button {
        - "min-width: 44px;"
        - "min-height: 44px;"
        - "display: inline-grid;"
        - "place-items: center;"
        - "}"
      what: 'The small class usually belongs to the glyph inside an adequately sized control (`<XIcon className="h-4 w-4" />` inside an `h-11` button), which matches whenever both sit on one line. Read the enclosing element: the hit target is the button''s box, not the icon''s.'
    "true":
      examples:
        - .icon-button {
        - "width: 18px;"
        - "height: 18px;"
        - "}"
      what: TODO
  instructions: "TODO: Touch targets need 44x44px (WCAG 2.5.5 Target Size Enhanced). 24x24px (WCAG 2.5.8 Target Size Minimum) is the floor only for dense desktop UI under `pointer: fine`; on touch it is a mistap generator, not a pass."
  type: noul
severity: minor
fix:
  hint: "TODO: Touch targets need 44x44px (WCAG 2.5.5 Target Size Enhanced). 24x24px (WCAG 2.5.8 Target Size Minimum) is the floor only for dense desktop UI under `pointer: fine`; on touch it is a mistap generator, "
  mode: none
status: draft
handWritten: []
portNotes:
  - detection is a shell pipeline or loop; port by hand
