# Ported from mblode/agent-skills skills/ui-design/rules/async-no-error-boundary.md by scripts/port-rules.ts.
# Keys listed in handWritten are preserved on re-run; everything else is regenerated.
id: interaction-no-error-boundary
title: Async tree without an error boundary
categoryId: state-coverage
domain: interaction
source:
  line: 10
  path: skills/ui-design/rules/async-no-error-boundary.md
  repo: mblode/agent-skills
  ruleId: async-no-error-boundary
  tier: release-blocker
scope:
  exclude:
    - "**/*.test.*"
    - "**/*.spec.*"
    - "**/*.stories.*"
  include:
    - "**/*.md"
    - "**/*.mdx"
    - "**/*.tsx"
    - "**/*.jsx"
unit:
  - paragraph
  - heading
  - jsx-text
  - attr-string
tier: jev
question:
  context:
    - element
    - docType
  criteria:
    "false":
      examples:
        - TODO
      what: |-
        - Skip leaf segments inherited from a parent that defines `error.tsx` (App Router cascades).
        - Skip components inside Storybook (`*.stories.tsx`).
        - Skip files annotated `// ui-audit-ignore:async-no-error-boundary`.
    "true":
      examples:
        - TODO
      what: TODO
  instructions: "TODO: A thrown error in a server component or client async tree with no ancestor error boundary unmounts the whole route: a blank page in production, a stack trace in dev. Every route segment and every independently-fetching widget needs its own boundary so one failure does not take the page down."
  type: noul
severity: major
fix:
  hint: "TODO: A thrown error in a server component or client async tree with no ancestor error boundary unmounts the whole route: a blank page in production, a stack trace in dev. Every route segment and every inde"
  mode: none
status: draft
handWritten: []
portNotes: []
