# Ported from mblode/agent-skills skills/ui-design/rules/slop-near-duplicate-scale.md by scripts/port-rules.ts.
# Keys listed in handWritten are preserved on re-run; everything else is regenerated.
id: craft-near-duplicate-scale
title: Near-duplicate of an existing scale step
categoryId: look-constraints
domain: craft
source:
  line: 10
  path: skills/ui-design/rules/slop-near-duplicate-scale.md
  repo: mblode/agent-skills
  ruleId: slop-near-duplicate-scale
  tier: backlog
scope:
  exclude:
    - "**/*.test.*"
    - "**/*.spec.*"
    - "**/*.stories.*"
    - "**/*.stories.tsx"
    - "**/*.test.tsx"
  include:
    - "**/*.tsx"
    - "**/*.jsx"
unit:
  - paragraph
  - heading
  - jsx-text
  - attr-string
tier: jev
question:
  context:
    - element
    - docType
  criteria:
    "false":
      examples:
        - TODO
      what: "Compare each hit against the theme's own values. Tailwind's defaults, if the theme has not overridden them:"
    "true":
      examples:
        - TODO
      what: TODO
  instructions: "TODO: A scale works because its steps are far enough apart to read as deliberate. `text-[15px]` sitting between `text-sm` (14px) and `text-base` (16px) does not create a new level of hierarchy, it creates a third size that looks like a mistake next to the other two. The same goes for `p-[13px]` beside `p-3`, or `rounded-[7px]` beside `rounded-md`. Users see labels that are almost but not quite aligned, "
  type: noul
severity: minor
fix:
  hint: "TODO: A scale works because its steps are far enough apart to read as deliberate. `text-[15px]` sitting between `text-sm` (14px) and `text-base` (16px) does not create a new level of hierarchy, it creates a"
  mode: none
status: draft
handWritten: []
portNotes:
  - detection is a shell pipeline or loop; port by hand
