# Ported from mblode/agent-skills skills/ui-design/rules/dark-i18n-untested.md by scripts/port-rules.ts.
# Keys listed in handWritten are preserved on re-run; everything else is regenerated.
id: craft-i18n-untested
title: Component lacks dark-mode coverage and hardcodes light tokens
categoryId: resilience
domain: craft
source:
  line: 10
  path: skills/ui-design/rules/dark-i18n-untested.md
  repo: mblode/agent-skills
  ruleId: dark-i18n-untested
  tier: backlog
scope:
  exclude:
    - "**/*.test.*"
    - "**/*.spec.*"
    - "**/*.stories.*"
  include:
    - "**/*.ts"
    - "**/*.tsx"
unit:
  - paragraph
  - heading
  - jsx-text
  - attr-string
tier: jev
question:
  context:
    - element
    - docType
  criteria:
    "false":
      examples:
        - TODO
      what: >-
        - Skip marketing-only directories (`app/(marketing)`) where the brand forbids dark mode; verify via the CSS entry file's `@theme` and `@custom-variant dark` lines, or a legacy `tailwind.config.*`.
        
        - Skip illustrations, brand SVGs, and logos where fixed color is intentional.
        
        - Skip `// ui-audit-ignor
    "true":
      examples:
        - TODO
      what: TODO
  instructions: "TODO: Hardcoded colors (`bg-white`, `text-black`, `#fff`, `text-gray-900`) break in dark mode: white surfaces glow on a dark background, fixed grays lose contrast, borders disappear. CSS-variable tokens (`bg-background`, `text-foreground`) make the same JSX work in both themes. The companion bug: no Storybook dark story or Chromatic dark snapshot, so the regression ships unnoticed."
  type: noul
severity: minor
fix:
  hint: "TODO: Hardcoded colors (`bg-white`, `text-black`, `#fff`, `text-gray-900`) break in dark mode: white surfaces glow on a dark background, fixed grays lose contrast, borders disappear. CSS-variable tokens (`b"
  mode: none
status: draft
handWritten: []
portNotes:
  - detection is a shell pipeline or loop; port by hand
