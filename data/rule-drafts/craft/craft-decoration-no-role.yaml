# Ported from mblode/agent-skills skills/ui-design/rules/slop-decoration-no-role.md by scripts/port-rules.ts.
# Keys listed in handWritten are preserved on re-run; everything else is regenerated.
id: craft-decoration-no-role
title: Stacked decorative layers with no role
categoryId: generic-decoration
domain: craft
source:
  line: 10
  path: skills/ui-design/rules/slop-decoration-no-role.md
  repo: mblode/agent-skills
  ruleId: slop-decoration-no-role
  tier: backlog
scope:
  exclude:
    - "**/*.test.*"
    - "**/*.spec.*"
    - "**/*.stories.*"
    - "**/*.stories.tsx"
    - "**/*.test.tsx"
  include:
    - "**/*.tsx"
    - "**/*.jsx"
unit:
  - paragraph
  - heading
  - jsx-text
  - attr-string
tier: jev
question:
  context:
    - element
    - docType
  criteria:
    "false":
      examples:
        - TODO
      what: "Every condition is a lookahead **inside one element match**, not a stage in a pipeline. That is load-bearing: `prettier-plugin-tailwindcss` wraps long class strings across lines, and these layers have the longest class strings on the page. A chain of line-based filters silently returns zero on a wra"
    "true":
      examples:
        - TODO
      what: TODO
  instructions: "TODO: A blurred blob top-left, a second blob bottom-right, a grid overlay, a noise layer, and a radial glow behind the heading: five absolutely positioned elements that contain nothing, mean nothing, and sit between the user and the content. Each was added to make the section feel less empty, and together they make text harder to read, wash out the contrast the copy needs, cost paint time on every scrol"
  type: noul
severity: minor
fix:
  hint: "TODO: A blurred blob top-left, a second blob bottom-right, a grid overlay, a noise layer, and a radial glow behind the heading: five absolutely positioned elements that contain nothing, mean nothing, and si"
  mode: none
status: draft
handWritten: []
portNotes:
  - detection is a shell pipeline or loop; port by hand
